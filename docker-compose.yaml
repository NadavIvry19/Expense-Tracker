version: '3.7'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb://mongo:27017/expeses_tracker
    depends_on:
      - mongo

  mongo:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  app-tests: 
    build: 
      context: .
      dockerfile: Dockerfile.test
    environment:
      - TEST_MONGODB_URI=mongodb://mongo:27017/expeses_tracker
    depends_on:
      - mongo

volumes:
  mongo_data:

